import ReactDOM from "react-dom/client";
import { App } from "./App";
import "@project/tailwind-config/global.css"

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <App />
);
